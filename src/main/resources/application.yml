spring:
  application:
    name: applicant-service
  datasource:
    url: jdbc:postgresql://localhost:5432/applicants
    username: appuser
    password: apppass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
    port: 8081
